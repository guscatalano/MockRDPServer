name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v0.1.0)'
        required: true

permissions:
  contents: write

jobs:
  release:
    name: Build + publish release asset
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Publish self-contained CLI (console)
        run: >
          dotnet publish src/MockRdp -c Release -r win-x64 --self-contained true
          -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true
          -o publish

      # The tray app is the run-this entry point (everything's in the menu). Same folder,
      # so a bare MockRdpCli.exe hands off to MockRdpTray.exe next to it.
      - name: Publish self-contained tray (GUI)
        run: >
          dotnet publish src/MockRdp.Tray -c Release -r win-x64 --self-contained true
          -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true
          -o publish

      # Published as release assets so consumers (RDPeek) can fetch stable URLs:
      #   https://github.com/guscatalano/MockRDPServer/releases/latest/download/MockRdpTray.exe   (run this)
      #   https://github.com/guscatalano/MockRDPServer/releases/latest/download/MockRdpCli.exe    (console/CI)
      - name: Create / update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: pwsh
        run: |
          $tag = if ('${{ github.event_name }}' -eq 'workflow_dispatch') { '${{ github.event.inputs.tag }}' } else { '${{ github.ref_name }}' }
          gh release create $tag `
            publish/MockRdpTray.exe `
            publish/MockRdpCli.exe `
            --title $tag --generate-notes
