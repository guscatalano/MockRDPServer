name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v0.1.0)'
        required: true

permissions:
  contents: write

jobs:
  release:
    name: Build + publish release asset
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Publish self-contained mock server (console)
        run: >
          dotnet publish src/MockRdp -c Release -r win-x64 --self-contained true
          -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true
          -o publish

      # The tray app is the no-cmdline entry point (run it, everything's in the menu). Same folder,
      # so a bare MockRdp.exe hands off to MockRdpTray.exe next to it.
      - name: Publish self-contained tray
        run: >
          dotnet publish src/MockRdp.Tray -c Release -r win-x64 --self-contained true
          -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true
          -o publish

      # Published as release assets so consumers (RDPeek) can fetch a stable URL:
      #   https://github.com/guscatalano/MockRDPServer/releases/latest/download/MockRdp.exe
      #   https://github.com/guscatalano/MockRDPServer/releases/latest/download/MockRdpTray.exe
      - name: Create / update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: pwsh
        run: |
          $tag = if ('${{ github.event_name }}' -eq 'workflow_dispatch') { '${{ github.event.inputs.tag }}' } else { '${{ github.ref_name }}' }
          gh release create $tag `
            publish/MockRdp.exe `
            publish/MockRdpTray.exe `
            --title $tag --generate-notes
